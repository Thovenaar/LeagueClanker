<Window x:Class="LeagueClanker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeagueClanker.App"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MainViewModel}"
        Title="LeagueClanker"
        Width="400" SizeToContent="Height" MaxHeight="920" MinWidth="360"
        Topmost="True" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResizeWithGrip" ShowInTaskbar="True"
        FontFamily="Segoe UI" Foreground="{StaticResource TextBrush}">

    <Window.Resources>
        <!-- Shared column layout for the stats tables: icon, name, level, AD, AP, armor, MR, HP, AS, crit -->
        <ControlTemplate x:Key="PlayerHeader" TargetType="ContentControl">
            <Grid Margin="4,0,4,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18" /><ColumnDefinition Width="*" /><ColumnDefinition Width="24" />
                    <ColumnDefinition Width="34" /><ColumnDefinition Width="34" /><ColumnDefinition Width="36" />
                    <ColumnDefinition Width="32" /><ColumnDefinition Width="42" /><ColumnDefinition Width="34" />
                    <ColumnDefinition Width="32" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="2" Style="{StaticResource StatHeader}" Text="lvl" />
                <TextBlock Grid.Column="3" Style="{StaticResource StatHeader}" Text="AD" />
                <TextBlock Grid.Column="4" Style="{StaticResource StatHeader}" Text="AP" />
                <TextBlock Grid.Column="5" Style="{StaticResource StatHeader}" Text="armor" />
                <TextBlock Grid.Column="6" Style="{StaticResource StatHeader}" Text="MR" />
                <TextBlock Grid.Column="7" Style="{StaticResource StatHeader}" Text="HP" />
                <TextBlock Grid.Column="8" Style="{StaticResource StatHeader}" Text="AS" />
                <TextBlock Grid.Column="9" Style="{StaticResource StatHeader}" Text="crit" />
            </Grid>
        </ControlTemplate>

        <DataTemplate x:Key="PlayerRowTemplate" DataType="{x:Type local:PlayerRow}">
            <Border Background="{StaticResource CardBrush}" CornerRadius="4" Padding="4,3" Margin="0,0,0,3" ToolTip="{Binding Note}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="18" /><ColumnDefinition Width="*" /><ColumnDefinition Width="24" />
                        <ColumnDefinition Width="34" /><ColumnDefinition Width="34" /><ColumnDefinition Width="36" />
                        <ColumnDefinition Width="32" /><ColumnDefinition Width="42" /><ColumnDefinition Width="34" />
                        <ColumnDefinition Width="32" />
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding IconUrl}" Width="18" Height="18" />
                    <TextBlock x:Name="Name" Grid.Column="1" Text="{Binding Name}" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="2" Style="{StaticResource StatCell}" Text="{Binding Level}" Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Grid.Column="3" Style="{StaticResource StatCell}" Text="{Binding AttackDamage, StringFormat={}{0:0}}" />
                    <TextBlock Grid.Column="4" Style="{StaticResource StatCell}" Text="{Binding AbilityPower, StringFormat={}{0:0}}" />
                    <TextBlock Grid.Column="5" Style="{StaticResource StatCell}" Text="{Binding Armor, StringFormat={}{0:0}}" />
                    <TextBlock Grid.Column="6" Style="{StaticResource StatCell}" Text="{Binding MagicResist, StringFormat={}{0:0}}" />
                    <TextBlock Grid.Column="7" Style="{StaticResource StatCell}" Text="{Binding Health, StringFormat={}{0:0}}" />
                    <TextBlock Grid.Column="8" Style="{StaticResource StatCell}" Text="{Binding AttackSpeed, StringFormat={}{0:0.00}}" />
                    <TextBlock Grid.Column="9" Style="{StaticResource StatCell}" Text="{Binding CritChance, StringFormat={}{0:0}%}" />
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsMe}" Value="True">
                    <Setter TargetName="Name" Property="Foreground" Value="{StaticResource AccentBrush}" />
                    <Setter TargetName="Name" Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
        <DockPanel>
            <!-- Title bar: drag to move -->
            <Grid DockPanel.Dock="Top" Height="34" Background="Transparent" MouseLeftButtonDown="OnTitleBarMouseDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="LeagueClanker" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="10,1,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,6,0">
                    <Button Style="{StaticResource ChromeButton}" Content="📌" ToolTip="Toggle always on top" Click="OnPinClick" />
                    <Button Style="{StaticResource ChromeButton}" Content="✕" ToolTip="Close" Click="OnCloseClick" />
                </StackPanel>
            </Grid>

            <TextBlock DockPanel.Dock="Bottom" Text="{Binding Footer}" Foreground="{StaticResource MutedBrush}" FontSize="10" Margin="12,4,12,8" />

            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12,0,12,0"
                          Visibility="{Binding IsLive, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <DockPanel Margin="0,2,0,8">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <RadioButton Style="{StaticResource TabButton}" Content="Build" GroupName="Tabs" IsChecked="{Binding ShowBuild}" />
                            <RadioButton Style="{StaticResource TabButton}" Content="Players" GroupName="Tabs" IsChecked="{Binding ShowPlayers}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{Binding ChampionLine}" FontSize="16" FontWeight="SemiBold" />
                    </DockPanel>

                    <!-- Enemy damage split bar -->
                    <TextBlock Text="{Binding DamageSummary}" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,4" />
                    <Grid Height="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding AdShare}" />
                            <ColumnDefinition Width="{Binding ApShare}" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{StaticResource AdBrush}" CornerRadius="3,0,0,3" />
                        <Border Grid.Column="1" Background="{StaticResource ApBrush}" CornerRadius="0,3,3,0" />
                    </Grid>

                    <!-- ===== Build tab ===== -->
                    <StackPanel Visibility="{Binding ShowBuild, Converter={StaticResource BoolToVisibility}}">

                        <!-- Pivot suggestion: accept or keep your build -->
                        <Border Visibility="{Binding HasPivot, Converter={StaticResource BoolToVisibility}}"
                                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="6"
                                Background="#1FC8AA6E" Padding="10,8" Margin="0,12,0,0">
                            <StackPanel>
                                <TextBlock Text="PIVOT SUGGESTED" Foreground="{StaticResource AccentBrush}" FontSize="10" FontWeight="Bold" />
                                <TextBlock Text="{Binding PivotSummary}" FontWeight="SemiBold" FontSize="13" TextWrapping="Wrap" Margin="0,3,0,4" />
                                <ItemsControl ItemsSource="{Binding PivotReasons}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontSize="11" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Style="{StaticResource PrimaryButton}" Content="Switch build" Click="OnAcceptPivot" />
                                    <Button Style="{StaticResource SecondaryButton}" Content="Keep mine" Click="OnDeclinePivot" Margin="8,0,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- After declining (or on small differences), switching stays one click away -->
                        <DockPanel Visibility="{Binding ShowSwitchHint, Converter={StaticResource BoolToVisibility}}" Margin="0,10,0,0">
                            <Button DockPanel.Dock="Right" Style="{StaticResource LinkButton}" Content="Switch anyway" Click="OnSwitchToLatest" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding SwitchHint}" Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,8,0" />
                        </DockPanel>

                        <TextBlock Style="{StaticResource SectionHeader}" Text="YOUR BUILD · MOST IMPORTANT FIRST" />
                        <ItemsControl ItemsSource="{Binding Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:ItemRow}">
                                    <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="8,4" Margin="0,0,0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="22" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Rank}" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" VerticalAlignment="Center" />
                                            <Image Grid.Column="1" Source="{Binding IconUrl}" Width="30" Height="30" Margin="0,0,10,0" />
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <DockPanel>
                                                    <TextBlock DockPanel.Dock="Right" Text="{Binding Gold, StringFormat={}{0}g}" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" />
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                </DockPanel>
                                                <ItemsControl ItemsSource="{Binding Reasons}" Margin="0,2,0,0">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel />
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="3" Padding="5,0" Margin="0,0,4,0">
                                                                <TextBlock Text="{Binding}" Foreground="{StaticResource AccentBrush}" FontSize="10" />
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Boots, only while you haven't bought tier 2 boots -->
                        <StackPanel DataContext="{Binding Boots}">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Style="{StaticResource SectionHeader}" Text="BOOTS" />
                            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding IconUrl}" Width="30" Height="30" Margin="22,0,10,0" />
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Gold, StringFormat={}  {0}g}" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <TextBlock Style="{StaticResource SectionHeader}" Text="WHY" />
                        <ItemsControl ItemsSource="{Binding Advice}" Margin="0,0,0,8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,0,0,6">
                                        <TextBlock DockPanel.Dock="Left" Text="•" Foreground="{StaticResource AccentBrush}" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12" />
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <!-- ===== Players tab ===== -->
                    <StackPanel Visibility="{Binding ShowPlayers, Converter={StaticResource BoolToVisibility}}" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource SectionHeader}" Text="ENEMIES" />
                        <ContentControl Template="{StaticResource PlayerHeader}" />
                        <ItemsControl ItemsSource="{Binding Enemies}" ItemTemplate="{StaticResource PlayerRowTemplate}" />

                        <TextBlock Style="{StaticResource SectionHeader}" Text="YOUR TEAM" />
                        <ContentControl Template="{StaticResource PlayerHeader}" />
                        <ItemsControl ItemsSource="{Binding Team}" ItemTemplate="{StaticResource PlayerRowTemplate}" />

                        <TextBlock Text="Your stats come from the game. Everyone else's are base stats at their level plus items. Hover a row for details."
                                   Foreground="{StaticResource MutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,8,0,0" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
